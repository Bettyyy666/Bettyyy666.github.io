# unseen.co 背景效果复刻:可行性分析与计划

## 一、视频里的效果拆解

从录屏帧分析,unseen.co 首页由这几层组成:

1. **实时 3D 梦境场景**(WebGL 实时渲染,非视频):粉色拱门、台阶、远山、水面、彩虹泡泡球体、漂浮尘埃粒子
2. **水面反射 + 涟漪**:反射场景并叠加波纹扰动
3. **鼠标视差**:相机随鼠标轻微移动,产生空间感
4. **滚动"融化"转场**:滚动时整个画面(含标题文字)被扭曲 shader 拉扯变形,过渡到下一区块 —— 说明标题文字也是渲染在 WebGL 里的,不是普通 DOM
5. **后期处理**:胶片颗粒、轻微色差、柔光

技术栈参考:Unseen 的惯用做法是把每个场景渲染到 render target,再用 fragment shader 做扭曲混合转场,动画时间线用 Theatre.js 管理。

## 二、你现有站点的情况

- 纯静态单页:`index.html`(内联样式)+ `support.js`(生成的运行时,**不可手改**)
- 已有固定定位的粉色 CSS 光斑背景(`#mesh-bg` + 4 个 orb),z-index 0
- Hero 有一个 2D canvas 粒子动画(`#hero-particles`)
- 主色 `#F2CBD9`,和 unseen 的粉色调天然契合
- GitHub Pages 托管,无构建工具 → three.js 用 CDN + importmap 引入即可

## 三、可行性结论

| 效果 | 可行性 | 说明 |
|---|---|---|
| 空间感动态背景(雾、天空渐变、漂浮粒子、视差) | ✅ 容易 | 纯程序化,不需要任何素材 |
| 水面反射 + 涟漪 | ✅ 中等 | three.js Reflector + 法线贴图 |
| 彩虹泡泡球体 | ✅ 中等 | MeshPhysicalMaterial 的 iridescence + transmission |
| 拱门/台阶/远山场景 | ⚠️ 较难 | 需要 3D 模型 + 烘焙光照贴图(核心缺口,见第五节) |
| 滚动融化转场(含文字) | ⚠️ 难 | 需把标题也画进 WebGL;折中方案:只扭曲背景 + DOM 文字淡出,能达到 90% 的感觉 |
| 胶片颗粒/色差后期 | ✅ 容易 | 一个 post-processing pass |

**重要说明**:不能直接抓取 unseen.co 的 3D 模型、贴图来用 —— 那是他们的版权资产。能复刻的是**技术手法和氛围**,场景素材需要自备或用免费素材重建。

## 四、复刻计划(分四阶段)

**阶段 0 · 基础设施(半天)**
importmap 引入 three.js(CDN),新增全屏固定 `<canvas>` 替换现有 `#mesh-bg` 光斑层,`pointer-events: none`,不动 support.js。

**阶段 1 · 空间感动态背景(1–2 天)— 建议先做**
粉色渐变天空穹顶 + 指数雾 + 漂浮尘埃粒子 + 鼠标视差相机 + 胶片颗粒 shader。零素材,立刻有"空间感"。

**阶段 2 · 场景元素(2–4 天)**
水面(Reflector + 涟漪法线贴图)、彩虹泡泡球、拱门/台阶(程序化几何体,或导入 GLB 模型)。

**阶段 3 · 滚动转场(2–3 天)**
场景渲染到 render target,滚动速度驱动扭曲 shader;DOM 内容同步淡入淡出。

**阶段 4 · 性能与降级(1 天)**
pixelRatio 上限 1.5、页面不可见时暂停、移动端降级为静态渐变、检测 WebGL 失败回退到现有 CSS 背景。

## 五、缺失素材清单

| 素材 | 用途 | 获取方式 |
|---|---|---|
| **3D 场景模型(GLB)**:拱门、台阶、地形 | 阶段 2 的核心 | ① 我用 three.js 几何体程序化搭建(免费,风格化);② Blender 自建;③ Sketchfab/Poly Haven 找 CC0 模型 |
| **烘焙光照贴图** | unseen 那种柔和粉色光影的关键 | 需要 Blender 烘焙,没有免费现成的 —— 这是最大缺口;不烘焙则用实时灯光近似 |
| 水面法线贴图 | 涟漪 | three.js 官方示例自带(MIT,免费) |
| 环境 HDRI | 反射/氛围光 | Poly Haven 免费下载 |
| 衬线斜体标题字体 | 如果想要那种字体气质 | 免费替代:Playfair Display / Instrument Serif(Google Fonts) |
| 颗粒/噪声纹理 | 后期 | shader 内程序化生成,不需要文件 |

**总结**:阶段 0+1 不缺任何素材,今天就能做;真正缺的只有「3D 场景模型 + 烘焙光照」,决定了最终像不像。建议先跑通阶段 1,再决定场景走程序化还是 Blender 路线。

---

# 六、细化方案:天空 → 入水 3D 场景(选定方向)

用两张参考图(粉色星空海面 / 水下波光)做「水上 hero → 滚动下潜 → 水下内容区」的叙事背景。

## 可行性

✅ 可行,且比复刻 unseen 原场景**更容易**——不用建模拱门台阶。关键原则:**图片只当氛围底,空间感靠实时元素**(真 3D 水面、粒子、雾、相机运动),否则只是一张会动的壁纸。

## 场景分层(three.js scene graph)

**水上(hero)**

| 层 | 内容 | 实现 |
|---|---|---|
| L1 天空背板 | 图 1 的天空+云部分 | 贴在远距离大弧面上,UV 缓慢漂移 + 色彩呼吸 + 鼠标视差(远层移动慢,产生纵深) |
| L2 真·水面 | 替代图 1 下半部分的静态海面 | three.js `Water`(waternormals 法线贴图)+ 粉色 tint + 反射天空 + 太阳反光带。**空间感的核心** |
| L3 星光粒子 | 替代图中烘焙的静态星星 | additive blend 闪烁粒子(静态星星在动态场景里会穿帮) |
| L4 前景 | 尘埃粒子 + 薄雾 | Points + FogExp2 |

**水下(内容区)**

| 层 | 内容 | 实现 |
|---|---|---|
| L5 水下雾 | 深度感 | 指数雾,颜色随深度 青粉 → 深粉紫 |
| L6 体积光束 | god rays | 数片透明渐变面片缓慢摆动(便宜且效果好) |
| L7 焦散光斑 | 图 2 里的波光 | 程序噪声 caustics shader,投在水底渐变面上 |
| L8 气泡 | 上升气泡 | InstancedMesh:少量折射大泡 + 大量发光小点 |
| (备选) | 图 2 作远景纹理 | 大幅面片 + ripple displacement shader;**优先程序化**,更有空间感 |

**全局后期**:film grain + 轻微色差 + vignette;入水瞬间一个 distortion pass。

## Scroll 时间线(滚动进度 p 驱动,全部 lerp 平滑)

- **p 0–0.15 · Hero**:相机在水面上方,鼠标视差,水面波动、星光闪烁
- **p 0.15–0.3 · 下潜**:相机 Y 下移逼近水面;**穿越瞬间**(~0.5s):白闪 + 气泡爆发 + 全屏扭曲 —— 对应 unseen 的"融化"转场记忆点
- **p 0.3–1.0 · 水下**:雾渐浓、光束/焦散/气泡持续;about/experience/publications/skills/contact 各 section 照常滚动淡入

## DOM 集成

- 全屏 fixed canvas(z-index 0,pointer-events none)替换现有 `#mesh-bg` + 4 个 orb
- 各 section 背景改透明/半透明磨砂卡片,文字保持 DOM(可选中、利于 SEO)
- 现有 `#hero-particles` 2D 粒子可移除(被 L3/L4 取代)
- 不碰 support.js;three.js 代码独立 module script,importmap CDN 引入

## 实施顺序与工期

1. 基建 + L1 天空背板 + 视差 — 0.5 天
2. L2 水面 + 反光 — 1 天 ← **出效果的关键节点**
3. L3/L4 粒子 — 0.5 天
4. 下潜相机 + 入水转场 — 1 天
5. 水下 L5–L8 — 1.5 天
6. 后期 + 降级(移动端/无 WebGL 回退静态图) — 0.5 天

合计约 5 天量级,每步做完都可上线预览。

## 本方案素材清单

| 素材 | 状态 | 说明 |
|---|---|---|
| 图 1 文件本体 | ❗缺 | 目前只在聊天里,需要导出图片文件放进 `assets/`;竖版 810×1440,桌面宽屏只用上半天空部分,**若有横版/更高清版本最好** |
| 图 2 | 够用 | 只当色彩/氛围参考(水下走程序化),不需要文件也行 |
| waternormals 法线贴图 | 免费 | three.js 官方示例自带 |
| 其余(粒子/焦散/光束/颗粒) | 无需 | 全部 shader 程序化 |

**风险**:竖图拉宽到桌面会糊或裁掉构图,最好补一张横版天空图;水面反射对低端机吃力,已含降级方案。
